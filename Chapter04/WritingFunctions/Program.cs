
/*ConfigureConsole(); // ConfigureConsole("it-IT", true); per usare la cultura del computer

//decimal TaxToPay = CalculateTax(amount: 149, twoLetterRegionCode: "FR");
//WriteLine($"You must pay {TaxToPay:C} in tax."); //:C per il dollare per questa cultura

//In alternativa, puoi usare:
Write("The value is: ");
string? value = ReadLine();
WriteLine();
Write("Enter the two-letter region code (e.g., CH, DK, GB, etc.): ");
string? str_value = ReadLine();

if (int.TryParse(value, out int number))
{
    WriteLine($"You must pay {CalculateTax(amount: number, twoLetterRegionCode: str_value):C} in tax.");
}

WriteLine("To exit a program, digit 'b'");
WriteLine();
*/

RunFibFunctional();
  


